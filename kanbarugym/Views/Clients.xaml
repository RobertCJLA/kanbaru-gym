<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:conv="clr-namespace:kanbarugym.Converters"
             x:Class="kanbarugym.Views.Clients"
             x:Name="ThisPage"
             Title="Lista de Clientes"
             BackgroundColor="White">

    <ContentPage.Resources>
        <ResourceDictionary>
            <conv:LastNameInitialConverter x:Key="LastNameInitialConverter" />
            <conv:AgeFromBirthdateConverter x:Key="AgeFromBirthdateConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Shell.TitleView>
        <Grid>
            <VerticalStackLayout Margin="0,10,15,0">
                <Label Text="Lista de clientes"
                   FontSize="22"
                   HorizontalTextAlignment="Center"
                   VerticalTextAlignment="Center"
                   FontAttributes="Bold" 
                   TextColor="Black"
                   HorizontalOptions="Fill"
                   VerticalOptions="Fill"/>
                <Border StrokeThickness="0"
                        BackgroundColor="#0595EC"
                        StrokeShape="RoundRectangle 100,100"
                        HeightRequest="5"
                        WidthRequest="165"
                        HorizontalOptions="Center" />

            </VerticalStackLayout>
        </Grid>
    </Shell.TitleView>

    <VerticalStackLayout Padding="20,10">

        <Grid HeightRequest="45">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>

            <Border Grid.ColumnSpan="3"
                    Stroke="#72C9F4"
                    StrokeThickness="1"
                    BackgroundColor="White"
                    Padding="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10"/>
                </Border.StrokeShape>

                <Border.Shadow>
                    <Shadow Brush="Black"
                            Opacity="0.1"
                            Offset="0,0"
                            Radius="5"/>
                </Border.Shadow>

            </Border>

            <Image Source="filter.png"
                   Grid.Column="0"
                   VerticalOptions="Center"
                   HorizontalOptions="Center"
                   WidthRequest="20"
                   HeightRequest="20"/>

            <Entry x:Name="searchEntry"
                   Grid.Column="1"
                   Placeholder="Buscar por nombre o apellidos"
                   BackgroundColor="Transparent"
                   VerticalOptions="Center"
                   Margin="0"
                   TextColor="Black"
                   Text="{Binding SearchText, Mode=TwoWay, UpdateSourceEventName=TextChanged}"/>

            <Button Grid.Column="2"
                    ImageSource="search.png"
                    WidthRequest="50"
                    HeightRequest="50"
                    BackgroundColor="Transparent"/>
        </Grid>

        
        <CollectionView x:Name="ClientesCollection"
                        Margin="0,10,0,0"
                        SelectionMode="None"
                        ItemsSource="{Binding Clientes}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout>
                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <Border BackgroundColor="#0595EC"
                                    Grid.Column="0"
                                        StrokeThickness="0"
                                        StrokeShape="Ellipse"
                                        HeightRequest="40"
                                        WidthRequest="40"
                                        VerticalOptions="Center"
                                        HorizontalOptions="Start">
                                <Label Text="{Binding Nombres, Converter={StaticResource LastNameInitialConverter}}"
                                           TextColor="White"
                                           FontAttributes="Bold"
                                           FontSize="14"
                                           HorizontalOptions="Center"
                                           VerticalOptions="Center"
                                           HorizontalTextAlignment="Center"
                                           VerticalTextAlignment="Center"/>
                            </Border>

                            <Label Text="{Binding Nombres}"
                                       FontAttributes="Bold"
                                   Grid.Column="1"
                                   Margin="5,0"
                                       FontSize="16"
                                       LineBreakMode="TailTruncation"
                                       VerticalOptions="Center">
                            </Label>

                            <Button Grid.Column="2"
                                    Padding="5"
                                    Clicked="OnMemberShipPage"
                                    ImageSource="settings.png"
                                    WidthRequest="30"
                                    HeightRequest="30"
                                    BackgroundColor="Transparent"
                                    VerticalOptions="Center"
                                    HorizontalOptions="Center"/>
                        </Grid>

                        <Border Margin="5,0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="10"/>
                            </Border.StrokeShape>
                        <VerticalStackLayout BackgroundColor="#0595EC" Padding="20,10">
                            
                            <Label Text="{Binding CorreoElectronico}" TextColor="{StaticResource White}" FontSize="18"/>
                            <Label Text="{Binding Telefono}" TextColor="{StaticResource White}" FontSize="18"/>
                            <Label Text="{Binding Sexo}" TextColor="{StaticResource White}" FontSize="18"/>
                            <Label Text="{Binding FechaNacimiento, Converter={StaticResource AgeFromBirthdateConverter}, StringFormat='Edad: {0} años'}" TextColor="{StaticResource White}" FontSize="18"/>
                            
                        </VerticalStackLayout>

                            
                        </Border>
                        
                    </VerticalStackLayout>
                    
                </DataTemplate>
                
            </CollectionView.ItemTemplate>
            
        </CollectionView>

    </VerticalStackLayout>
    
</ContentPage>